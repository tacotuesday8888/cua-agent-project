# CLAUDE.md

Guidance for AI coding agents working in this repository.

## Agent skills

### Issue tracker

Issues are tracked in **GitHub Issues** on `tacotuesday8888/cua-agent-project`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label vocabulary — the label strings equal the five canonical triage role names (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.
